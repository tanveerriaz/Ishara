# Cloudflare Pages — GitHub integration
# Root directory: web
# Build command: npm run build
# Output directory: dist
# (prebuilt public/data is committed so CI needs no Python rebuild)

name = "ishara"
compatibility_date = "2026-08-02"
